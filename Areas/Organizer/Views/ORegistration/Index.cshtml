@{
    Layout = "~/Areas/Organizer/Views/Shared/_LayoutOrganizer.cshtml";
    var ls = ViewBag.listReg as List<EventSphere.Models.ModelViews.RegistrationView>;
}
<main>
    <div class="container-fluid px-4">
        <h1 class="mt-4">Registrations</h1>
        @Html.AntiForgeryToken()

        <div class="card mb-4">
            <div class="card-header">
                <i class="fas fa-table me-1"></i> Registration List
            </div>
            <div class="card-body">
                <table id="datatablesSimple" class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>No</th>
                            <th>Student</th>
                            <th>Event</th>
                            <th>Status</th>
                            <th>Register On</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (ls != null)
                        {
                            int i = 1;
                            foreach (var item in ls)
                            {
                                <tr data-id="@item.Id">
                                    <td>@i</td>
                                    <td>@item.StudentEmail</td>
                                    <td>@item.EventName</td>
                                    <td class="col-status">
                                        @(item.Status == 0 ? "Pending" :
                                            item.Status == 1 ? "Accepted" : "Denied")
                                    </td>
                                    <td>@item.RegisterOn</td>
                                    <td class="col-action">
                                        @if (item.Status == 0)
                                        {
                                            <button class="btn btn-success btn-sm btn-accept" data-id="@item.Id">Accept</button>
                                            <button class="btn btn-danger btn-sm btn-deny" data-id="@item.Id">Deny</button>
                                        }
                                        else
                                        {
                                            <button class="btn btn-secondary btn-sm" disabled>
                                                @(item.Status == 1 ? "Accepted" : "Denied")
                                            </button>
                                        }
                                    </td>
                                </tr>
                                i++;
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</main>

@section Scripts {
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <!-- DataTables -->
    <link rel="stylesheet"
          href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css" />
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>

    <script>
        // Khởi tạo DataTables
        $(document).ready(function () {
            $('#datatablesSimple').DataTable({
                "pageLength": 10,
                "lengthMenu": [5, 10, 25, 50],
                "ordering": true,
                "searching": true,
                "language": {
                    "lengthMenu": "Show _MENU_ Records",
                    "zeroRecords": "can't find result",
                    "info": "Trang _PAGE_ / _PAGES_",
                    "infoEmpty": "No data",
                    "infoFiltered": "(filter _MAX_ record)",
                    "search": "Search:",
                    "paginate": {
                        "first": "First",
                        "last": "Last",
                        "next": "Next",
                        "previous": "Previous"
                    }
                }
            });
        });

        // Accept / Deny AJAX
        (function () {
            $(document).off('click', '.btn-accept').on('click', '.btn-accept', function (e) {
                e.preventDefault();

                const $btn = $(this);
                const id = $btn.data('id');
                const $row = $btn.closest('tr');
                const $deny = $row.find('.btn-deny');

                if ($btn.data('sending')) return;

                const token = $('input[name="__RequestVerificationToken"]').val();
                if (!token) {
                    alert('Can not find __RequestVerificationToken.');
                    return;
                }

                $btn.data('sending', true);
                $btn.prop('disabled', true);
                $deny.prop('disabled', true);
                const origHtml = $btn.html();
                $btn.html('<span class="spinner-border spinner-border-sm"></span> Loading');

                $.ajax({
                    url: '/Organizer/ORegistration/ApproveAndSend',
                    type: 'POST',
                    data: {
                        id: id,
                        __RequestVerificationToken: token
                    },
                    success: function (res) {
                        if (res && res.success) {
                            alert(res.message || 'Success.');
                            // cập nhật row
                            $row.find('.col-status').html('<span class="text-success">Accepted</span>');
                            $row.find('.col-action').html('<button class="btn btn-secondary btn-sm" disabled>Accepted</button>');
                        } else {
                            alert(res?.message || 'Fail.');
                            $btn.prop('disabled', false);
                            $deny.prop('disabled', false);
                            $btn.data('sending', false);
                            $btn.html(origHtml);
                        }
                    },
                    error: function (xhr) {
                        alert('Server Error: ' + (xhr.responseText || 'Unknown error'));
                        $btn.prop('disabled', false);
                        $deny.prop('disabled', false);
                        $btn.data('sending', false);
                        $btn.html(origHtml);
                    }
                });
            });

            $(document).off('click', '.btn-deny').on('click', '.btn-deny', function (e) {
                e.preventDefault();
                if (!confirm('Are you sure to reject')) return;

                const $btn = $(this);
                const id = $btn.data('id');
                const $row = $btn.closest('tr');
                const $accept = $row.find('.btn-accept');

                if ($btn.data('sending')) return;

                const token = $('input[name="__RequestVerificationToken"]').val();
                if (!token) {
                    alert('Cannot find __RequestVerificationToken.');
                    return;
                }

                $btn.data('sending', true);
                $btn.prop('disabled', true);
                $accept.prop('disabled', true);
                const origHtml = $btn.html();
                $btn.html('<span class="spinner-border spinner-border-sm"></span> Đang xử lý');

                $.ajax({
                    url: '/Organizer/ORegistration/Deny',
                    type: 'POST',
                    data: {
                        id: id,
                        __RequestVerificationToken: token
                    },
                    success: function (res) {
                        if (res && res.success) {
                            $row.find('.col-status').html('<span class="text-danger">Denied</span>');
                            $row.find('.col-action').html('<button class="btn btn-secondary btn-sm" disabled>Denied</button>');
                            alert(res.message || 'Rejected.');
                        } else {
                            alert(res?.message || 'Can not reject.');
                            $btn.prop('disabled', false);
                            $accept.prop('disabled', false);
                            $btn.data('sending', false);
                            $btn.html(origHtml);
                        }
                    },
                    error: function (xhr) {
                        alert('Server Error: ' + (xhr.responseText || 'Unknown error'));
                        $btn.prop('disabled', false);
                        $accept.prop('disabled', false);
                        $btn.data('sending', false);
                        $btn.html(origHtml);
                    }
                });
            });
        })();
    </script>
}
